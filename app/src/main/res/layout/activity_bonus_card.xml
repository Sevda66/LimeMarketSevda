<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#DC7878"
    android:backgroundTint="#FDFDFD">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Заголовок -->
        <TextView
            android:id="@+id/tv_bonus_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:text="Бонусная карта"
            android:textSize="26sp"
            android:textStyle="bold" />

        <!-- Картинка как кнопка -->
        <ImageView
            android:id="@+id/iv_bonus_image"
            android:layout_width="285dp"
            android:layout_height="148dp"
            android:src="@drawable/card" /> <!-- Тут будет изображение -->


        <!-- Баланс -->
        <TextView
            android:id="@+id/tv_bonus_balance"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:text="Ваши бонусы:0"
            android:textSize="24sp" />


        <Button
            android:id="@+id/btn_logout"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:backgroundTint="#CE0202"
            android:text="Выйти"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textSize="18sp" />
    </LinearLayout>
</ScrollView>
